export class MySeriesList {
    constructor(id, series, status, stars, comment)
    {
        this.id = id;
        this.series = series;
        this.status = status;
        this.stars = stars;
        this.comment = comment;
    }
}