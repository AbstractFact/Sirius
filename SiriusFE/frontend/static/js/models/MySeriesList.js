export class MySeriesList {
    constructor(id, series, status, stars, comment, favourite)
    {
        this.id = id;
        this.series = series;
        this.status = status;
        this.stars = stars;
        this.comment = comment;
        this.favourite = favourite;
    }
}